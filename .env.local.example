# Azure OpenAI-compatible endpoint configuration
# Use GITHUB_TOKEN for Azure endpoint (as shown in your previous projects)
GITHUB_TOKEN=your_github_token_here

# Optional: Override base URL if using a different endpoint
# OPENAI_BASE_URL=https://models.inference.ai.azure.com

# Optional: Override model (default: gpt-4o)
# OPENAI_MODEL=gpt-4o
